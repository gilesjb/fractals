%! Strange Attractor

/x 0.4 def
/y 0 def
/xx 0 def
/yy 0 def
/xy 0 def

/multiples { /xx x dup mul def /yy y dup mul def /xy x y mul def } bind def

/xcoefs {  0.7 -0.2 -0.8 -1.0 -0.1  0.1 } def

/ycoefs { -0.1  0.4  1.2 -1.0 -0.5  0.2 } def

/poly { yy mul exch y mul add exch xy mul add exch xx mul add exch x mul add add } bind def

/iterate { xcoefs poly ycoefs poly /y exch def /x exch def multiples } bind def

260 260 translate

/point 0.05 360 dup dup scale div def

/plot { x y point dup rectfill } bind def

100 {iterate} bind repeat 
5000000 {plot iterate} bind repeat
showpage
